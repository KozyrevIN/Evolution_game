#version 140

uniform sampler2D 	source;
uniform vec2        offsetFactor; 		

void main()
{
	vec2 textureCoordinates = gl_TexCoord[0].xy;
	vec4 color = texture2D(source, textureCoordinates);

	float alpha;

	alpha = texture2D(source, textureCoordinates + vec2(-5.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-5.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.055902;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.055902;
    alpha = texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.060634;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.060634;
    alpha = texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.062500;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.062500;
    alpha = texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.060634;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.060634;
    alpha = texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.055902;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.055902;
    alpha = texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-4.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.058926;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.058926;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.069338;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.069338;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.079057;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.079057;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.083333;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.083333;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.079057;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.079057;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.069338;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.069338;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.058926;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.058926;
    alpha = texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-3.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.055902;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.055902;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.069338;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.069338;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.088388;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.088388;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.111803;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.111803;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.125000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.125000;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.111803;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.111803;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.088388;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.088388;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.069338;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.069338;
    alpha = texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.055902;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-2.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.055902;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.060634;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.060634;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.079057;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.079057;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.111803;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.111803;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.176777;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.176777;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.250000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.250000;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.176777;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.176777;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.111803;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.111803;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.079057;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.079057;
    alpha = texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.060634;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(-1.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.060634;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -5.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -5.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.062500;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.062500;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.083333;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.083333;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.125000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.125000;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.250000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.250000;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.250000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.250000;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.125000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.125000;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.083333;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.083333;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.062500;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.062500;
    alpha = texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 5.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(0.0 * offsetFactor.x, 5.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.060634;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.060634;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.079057;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.079057;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.111803;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.111803;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.176777;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.176777;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.250000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.250000;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.176777;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.176777;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.111803;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.111803;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.079057;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.079057;
    alpha = texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.060634;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(1.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.060634;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.055902;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.055902;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.069338;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.069338;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.088388;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.088388;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.111803;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.111803;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.125000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.125000;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.111803;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.111803;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.088388;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.088388;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.069338;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.069338;
    alpha = texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.055902;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(2.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.055902;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, -4.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.058926;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.058926;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.069338;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.069338;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.079057;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.079057;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.083333;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.083333;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.079057;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.079057;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.069338;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.069338;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.058926;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.058926;
    alpha = texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(3.0 * offsetFactor.x, 4.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, -3.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.055902;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, -2.0 * offsetFactor.y)).a * 0.055902;
    alpha = texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.060634;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, -1.0 * offsetFactor.y)).a * 0.060634;
    alpha = texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.062500;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.062500;
    alpha = texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.060634;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, 1.0 * offsetFactor.y)).a * 0.060634;
    alpha = texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.055902;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, 2.0 * offsetFactor.y)).a * 0.055902;
    alpha = texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(4.0 * offsetFactor.x, 3.0 * offsetFactor.y)).a * 0.050000;
    alpha = texture2D(source, textureCoordinates + vec2(5.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.050000;
    color.a += alpha;
    color.rgb += alpha * texture2D(source, textureCoordinates + vec2(5.0 * offsetFactor.x, 0.0 * offsetFactor.y)).a * 0.050000;

	gl_FragColor = color;
}